import { CSESoc } from "../image/imageIndex";
import { FooterWrapper, Logo } from "./style";

const Footer = () => {
  return (
    <FooterWrapper>
      <Logo src={CSESoc} />
    </FooterWrapper>
  );
};

export default Footer;

import styled from "styled-components";

export const CardContentsTag = styled.div`
    border-radius: 8px;
    background-color: rgba(254, 227, 128);
    display: inline-block;
    margin: 0.2rem;
    padding: 0.25rem 0.4rem;
    width: auto;
`;
